name: 'thaifin_dbt'
version: '1.0.0'
config-version: 2

profile: 'thaifin_sqlite'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  thaifin_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
